# =============================================================================
# ROBOTIKO v2.0 — .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# INTERNAL DOCUMENTS (Not for public repo)
# -----------------------------------------------------------------------------
_management/creator_strategy.md
_management/*_quality_report.md

# -----------------------------------------------------------------------------
# SECRETS & CREDENTIALS (NEVER commit these)
# -----------------------------------------------------------------------------
.env
.env.local
.env.*.local
credentials.json
gcp-oauth.keys.json
tokens.json
*.pem
*.key
*.p12
*.pfx
aws-credentials
.aws/
secrets/

# -----------------------------------------------------------------------------
# VIDEO & AUDIO (Too large for Git — stored on Google Drive)
# -----------------------------------------------------------------------------
*.mp4
*.mov
*.avi
*.mkv
*.mp3
*.wav
*.flac
*.aac
*.ogg

# -----------------------------------------------------------------------------
# LARGE IMAGES (stored on Google Drive)
# -----------------------------------------------------------------------------
*.psd
*.ai
*_4k.png
*_8k.png

# All generated assets — raw and selected — go to Google Drive, not Git
episode-*/04_visuals/raw/
episode-*/04_visuals/selected/
episode-*/05_video/raw/
episode-*/05_video/selected/
episode-*/02_music/*.wav
episode-*/02_music/*.mp3

# Frame-curation staging (full-res originals dropped here by the curator;
# processed low-res frames land in _assets/style/frames/ which IS tracked)
_curation_staging/

# -----------------------------------------------------------------------------
# PYTHON
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
*.egg-info/
dist/
build/
.pytest_cache/

# -----------------------------------------------------------------------------
# NODE (if needed in future)
# -----------------------------------------------------------------------------
node_modules/
npm-debug.log*

# -----------------------------------------------------------------------------
# IDEs & EDITORS
# -----------------------------------------------------------------------------
.vscode/*
!.vscode/extensions.json
.claude/settings.local.json
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# -----------------------------------------------------------------------------
# OS FILES
# -----------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
Thumbs.db
Desktop.ini

# -----------------------------------------------------------------------------
# LOCAL-ONLY FILES (not for sharing)
# -----------------------------------------------------------------------------
_management/notebooklm_brief.md
_management/platform_research_*.md

# Personal/local utility scripts (not project-related)
_tools/*.ps1
_assets/banner_test.png

# Local walkthrough documents (operational, not shared)
episode-*/07_social_media/*_walkthrough.md

# Build-along tutorial runbooks (local-only — the videos are shared, not these working files)
_tutorial/

# -----------------------------------------------------------------------------
# TEMP & LOGS
# -----------------------------------------------------------------------------
*.tmp
*.log
*.cache
.cache/
temp/
tmp/

# -----------------------------------------------------------------------------
# GIT LFS TRACKING (large selected assets)
# -----------------------------------------------------------------------------
# If using Git LFS, track these instead of ignoring:
# *.png filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
# -----------------------------------------------------------------------------
# EXTERNAL LLM BASE FILES (claude.ai project-knowledge mirror - not repo content)
# -----------------------------------------------------------------------------
robotiko-v2-base files/

# -----------------------------------------------------------------------------
# PRIVATE PLAYBOOKS (never ship in the public repo)
# -----------------------------------------------------------------------------
_private/

# -----------------------------------------------------------------------------
# LAUNCH STRATEGY (local-only go-to-market research — never ship)
# -----------------------------------------------------------------------------
_launch/
